// typeof quirks(e.g.,typeof null = 'object')

// quirk is basically some wierd things in JS like the data type  null's type, in actual  is object. but it's still primitive so it's confusing and wierd but for now we just have to remember these kinds of few quirks. and some of them are here: 

// 1 + "1" = 11; 
// typeof NaN is Number  // NaN clearly means that Not a Number but it's type is still Number!
// and so much more... so I have to search them and remember them