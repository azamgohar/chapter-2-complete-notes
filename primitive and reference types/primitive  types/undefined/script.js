

// Undefined ✅
// If we have created a variable without initializing it's value so the variable exists but because we didn't gave any value to that, JS declare  it's value as undefined. Js itself gives it a value undefined because JS don't let any variable stay without a value so it gaves every non-initialized variable a value undefined.